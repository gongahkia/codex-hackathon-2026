(A) Implement auto-render executor for MP4 artifact with timeout guard in `app/video/render_executor.py` +video +remotion +runtime
(A) Implement video fallback that outputs config and command when render dependencies are missing in `app/video/render_executor.py` +video +remotion +runtime
(A) Implement run artifact registry storing paths for matrix, project, tests, and video in `app/storage/artifacts.py` +storage +output
(A) Implement SQLite run store for config, state transitions, and final status in `app/storage/run_store.py` +storage +db
(A) Implement cache layer for source query results keyed by normalized prompt in `app/storage/cache.py` +storage +cache +research
(A) Implement cache TTL policy that expires stale source results in `app/storage/cache.py` +storage +cache +research
(A) Implement structured JSON logger with run_id correlation in `app/telemetry/logging.py` +telemetry +core
(A) Implement phase timing collector for each pipeline stage in `app/telemetry/timings.py` +telemetry +budget
(A) Implement progress event emitter for CLI status updates in `app/telemetry/progress.py` +telemetry +cli
(A) Implement retry policy wrapper with jitter for source adapters in `app/resilience/retry.py` +resilience +research
(A) Implement circuit breaker for repeatedly failing source adapters in `app/resilience/circuit_breaker.py` +resilience +research
(A) Implement per-source timeout guard to prevent research starvation in `app/resilience/timeouts.py` +resilience +research +budget
(A) Implement sandboxed command allowlist for build and test commands in `app/security/command_policy.py` +security +runtime
(A) Implement secret redaction filter for logs in `app/security/redaction.py` +security +telemetry
(A) Implement final output assembler in `app/output/final_report.py` with summary, matrix, implementation, tests, and video sections +output +core
(A) Implement machine-readable JSON output writer in `app/output/final_report.py` +output +core
(A) Implement non-zero exit codes for terminal failure states in `app/cli.py` +cli +core
(B) Implement unit tests for `RunConfig` validation boundaries in `tests/test_run_config.py` +testing +unit
(B) Implement unit tests for weight normalization and negative input rejection in `tests/test_scoring_weights.py` +testing +unit
(B) Implement unit tests for deadline budget splitting invariants in `tests/test_time_budget.py` +testing +unit
(B) Implement unit tests for evidence gate pass and fail paths in `tests/test_evidence_gate.py` +testing +unit
(B) Implement integration test for research to ranking pipeline with mocked sources in `tests/test_pipeline_research_ranking.py` +testing +integration
(B) Implement integration test for Remotion fallback when renderer is unavailable in `tests/test_video_fallback.py` +testing +integration
(B) Implement end-to-end CLI test that runs full pipeline with fixture adapters in `tests/test_cli_e2e_run.py` +testing +e2e
(B) Implement end-to-end CLI test that verifies Reddit opt-in disclaimer enforcement in `tests/test_cli_reddit_optin.py` +testing +e2e
